import { AppBar, Paper, styled } from "@mui/material";

export const AppbarSearch = styled(AppBar)(({ theme }) => ({
  borderRadius: 4,
  marginBottom: "1rem",
  display: "flex",
  padding: "16px",
}));

export const PaperPag = styled(Paper)(({ theme }) => ({
  borderRadius: 4,
  marginTop: "1rem",
  padding: "16px",
}));
